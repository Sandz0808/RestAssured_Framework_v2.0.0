

========= EXECUTION  =====================================================
// execute in default env and selected groups
mvn clean test -Dgroups=debug

// execute in selected env and selected groups
mvn clean test -Denv=staging -Dgroups=debug

// EXECUTE WITH DEFINED APPLICATION
mvn clean test -Dapp=contactlist -Dconfig=staging -Dgroups=test

=======================================================================



// --- RUN IN ALLURE

# Run your tests (with mvn test
mvn clean test

# Generate Allure report
allure serve target/allure-results

# Generate static report
allure generate target/allure-results --clean -o target/allure-report

allure generate target/allure-results --single-file --output target/allure-report

# Generate HTML report
allure generate target/allure-results --single-file --output target/allure-report
allure generate target/allure-results --single-file --output target/allure-report --clean
Output in target/allure-report/index.html


// --- CLEAN OUTPUT DIRECTORY
mvn clean
rm -rf target/allure-results


Remove in TestRunner.java:
"html:target/cucumber-html-report",
Replace with:
"io.qameta.allure.cucumber7jvm.AllureCucumber7Jvm“

================================================================
ALLURE REPORTING - NEED IN GRAPH

// -- COPY THE ALLURE HISTORY
xcopy /E /I /Y target\allure-report\history target\allure-results\history


// IT SHOULD BE RUN TO GET THE HISTORY
allure generate target/allure-results --output target/allure-report

// TO COPY THE HISTORY - RUN IN TERMINAL
Copy-Item -Path "target/allure-report/history" `
          -Destination "target/allure-results/history" `
          -Recurse -Force

==========================================================
ALLURE HISTORY GUIDE
==========================================================
Step 1 — Run tests
mvn clean test -Dapp=plaid -Dconfig=staging -Dgroups=plaid

Step 2
New-Item -ItemType Directory -Force .\target\allure-results\history

Step 3
Copy-Item .\allure-history\history\* .\target\allure-results\history\ -Recurse -Force

Step 4
allure generate .\target\allure-results --clean -o .\target\allure-report

Step 5
Copy-Item .\target\allure-report\history\* .\allure-history\history\ -Recurse -Force

Step 6
Get-Content .\allure-history\history\history-trend.json

allure open .\target\allure-report


// For the automatic report generation + archive, use:

mvn clean verify -Dapp=plaid -Dconfig=staging -Dgroups=plaid
mvn clean verify -Dconfig=staging -Dgroups=test